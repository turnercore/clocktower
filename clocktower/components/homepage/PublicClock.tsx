// Clock that reads and subscribes to a specific database table called public
// This will allow anyone to update the hompeage clock (it will omit name and will have a limit of segments from 2 - 12)
import React from 'react'

const PublicClock: React.FC = () => {
  // Component logic here

  return <span>🕘</span>
}

export default PublicClock
